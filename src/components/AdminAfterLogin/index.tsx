'use client'

import React from 'react'

/** Shown beneath the Payload login actions (Forgot password / Login). */
export default function AdminAfterLogin() {
  return (
    <div className="chilmund-login-return">
      <a className="chilmund-login-return__link" href="/">
        ← Return to website
      </a>
    </div>
  )
}
