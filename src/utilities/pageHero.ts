/**
 * Shared compact dark hero bands — replaces tall py-20/md:py-28 stacks with optional top icons.
 */

/** Slate-950 + subtle blue radial (product / logistics / plant / uses / meet-the-team pattern). */
export const innerHeroRadialSection =
  'relative overflow-hidden bg-slate-950 px-4 py-10 text-center text-white sm:py-12 md:py-14'

/** About: logo + headline row — tighter than legacy py-16 md:py-28. */
export const aboutHeroSection =
  'relative overflow-hidden bg-slate-950 px-4 py-12 text-white md:py-16'

/** Contact / Events / Regional markets gradient shell. */
export const innerHeroGradientOuter =
  'relative bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 text-white'

export const innerHeroGradientInner =
  'container relative z-10 px-4 py-10 text-center sm:py-12 md:py-14'

/** News hub header strip. */
export const innerHeroNewsHeader =
  'border-b border-white/10 bg-slate-900/50 px-4 py-9 text-center md:py-10'
